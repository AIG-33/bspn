"use client";

import { AdminStub } from "@/components/sections/admin-stub";
import { Settings } from "lucide-react";

export default function Page() {
  return <AdminStub icon={Settings} titleKey="settings" />;
}
